# auto-accounting

## Run
- Install Java JDK 17+.
- On Windows, just double-click `run.bat`.
- Or run manually:

```bat
javac *.java
java MainFrame
```

To use this on another device, copy the whole folder and make sure `java` and `javac` are installed.